# 複製這個檔案為 .env（本機用），或在 PaaS 平台的環境變數設定裡填入這些值。
# 注意：.env 不要上傳到 GitHub（.gitignore 已排除）。

# 後台管理密碼。對外公開部署時「務必」設定，否則任何人都能上傳檔案、竄改字典。
# 純內網使用可留空（留空 = 不啟用驗證）。
ADMIN_PASSWORD=請改成你自己的強密碼

# 後台帳號（選填，預設 admin）
ADMIN_USER=admin

# 埠號（選填）。PaaS 平台會自動指定，本機不設則預設 3000。
# PORT=3000
